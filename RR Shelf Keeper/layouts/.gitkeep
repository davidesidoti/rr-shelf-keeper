# Per-save layout files (<saveKey>.lua) are written here by `rrshelf save` (Phase 2).
# This folder is committed (via .gitkeep) so it exists at runtime through the Mods junction —
# store.save() does not create directories. Generated *.lua layout files are gitignored.
